{% extends 'templates/layoutWithoutSidebar.twig' %}

{% block body %}
    <div class="position-absolute top-50 start-50 translate-middle">
        <p class="text-center fw-bold errorCode">{{ code }}</p>
        <p class="text-center fw-bold fs-1">{{ message }}</p>
    </div>
{% endblock %}